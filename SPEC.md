# 案件分案抽籤系統 規格書（SPEC）

| 項目 | 內容 |
|---|---|
| 文件版本 | v1.0 |
| 制定日期 | 2026-08-04 |
| 系統代號 | court-lottery |
| 適用範圍 | 金訴案、金重訴案之分案抽籤 |
| 狀態 | 草案，待需求方確認第 15 章「待確認事項」 |

---

## 1. 系統目的

金訴、金重訴案件的**承辦股已經確定**；本系統以電腦抽籤決定**由哪一個股支援**該案，
取代人工抽籤，達成下列目標：

1. **公平** — 抽籤結果由公共亂數決定，任何一方（含操作者、系統管理者、開發者）皆無法預知或操控。
2. **公開** — 所有抽籤紀錄與籤筒狀態即時公開，免登入即可查閱。
3. **可驗證** — 任何第三人可獨立重算並驗證每一筆抽籤結果，無須信任本系統。
4. **可稽核** — 歷史紀錄只增不刪，任何竄改都會留下痕跡。
5. **易讀** — 大字體、高對比、視覺化版面，適合年長使用者閱讀操作。
6. **權責分離** — 抽籤執行權可個別授權、個別撤銷，且授權者「只能發動抽籤，不能修改任何資料」。

---

## 2. 名詞定義

| 名詞 | 定義 |
|---|---|
| **承辦股** | 案件的承辦單位。**已經確定**，於抽籤時與案號一併輸入，不是抽出來的。 |
| **支援股** | 支援承辦股辦理該案的單位。**本系統抽的就是這個。**<br>每個支援股隸屬於一個庭。數量可增減，名稱可設定。 |
| **庭** | 支援股的上層單位（如第十六庭）。數量可增減，名稱可設定。 |

> **承辦股與支援股是兩組不相交的名單。** 承辦股不在支援股的籤筒內，
> 因此不會被抽為自己案件的支援股，也不需要特別排除。
>
> 設定檔中 `units` 指的是**支援股**（籤筒輪分的對象），`requesters` 指的是**承辦股**。
> 本文件其餘部分若單稱「股」，一律指支援股。
| **案類** | 抽籤的獨立類別。初始為「金訴」與「金重訴」，各有獨立籤筒，互不影響。可擴充。 |
| **籤** | 一支籤代表某股的一次受分案機會。 |
| **籤筒（bin）** | 某案類目前尚未被抽出的籤的集合。 |
| **每輪籤數 N** | 每個股的屬性。補籤時該股放入籤筒的籤數，預設 1。 |
| **補籤** | 觸發條件成立時，一次把所有在職股的籤依其 N 值放入籤筒。 |
| **抵分** | 重大案件折抵多件案量。抵 M 件 = 本案算 1 件，另需扣除該股 M−1 支籤。 |
| **欠籤（carryOver）** | 抵分時籤筒內存量不足以扣完，剩餘應扣支數記為該股欠籤，於下次補籤後優先扣除。 |
| **迴避** | 特定股本次不參與抽籤（其籤留在籤筒內，僅本次不可被抽中）。 |
| **輪（cycle）** | 兩次補籤之間的期間。 |

---

## 3. 抽籤規則（核心規範）

> 本章是全系統的規範核心。實作與測試皆以本章為準。

### 3.1 基本設定

- 初始 4 庭、8 股，每股每輪籤數 N = 1。
- 每個案類各有一個獨立籤筒。初始化時各投入 8 支籤（每股 1 支）。
- 抽籤時從籤筒內**現存的籤**中等機率隨機抽出 1 支。
  - 說明：若某股籤筒內有 2 支籤，其被抽中機率即為其他股的 2 倍。這是規則刻意的設計結果，非缺陷。

### 3.2 籤筒的正規化表示

籤筒在資料中以**陣列**儲存，並在每次異動後依下列規則重新排序（正規化）：

1. 依該籤所屬股在 `config.units` 中的 `order` 由小到大排序。
2. 同一股的多支籤相鄰排列。

正規化的目的：使「第 k 個位置對應哪一支籤」成為確定值，第三人才能用公開的亂數獨立重算抽籤結果。**此排序不影響公平性**，因為抽出的位置由不可預測的公共亂數決定。

### 3.3 單次抽籤流程

輸入：案類 `T`、案號 `caseNo`、抵分設定 `offsetMap`、迴避股清單 `excluded[]`

```
步驟 1  計算可抽集合
        前置檢查：若「全部在職股」皆列於 excluded[]
                  → 中止抽籤，回報「所有在職股均已迴避，無法抽籤」

        drawable = 籤筒 T 中，所屬股不在 excluded[] 的所有籤
        while drawable 為空:                       # 須為迴圈，不可只補一次
            執行一次「補籤」（理由記為 exclusion-exhausted）
            接續執行補籤檢查迴圈（步驟 4 的邏輯）
            重新計算 drawable
            若補籤次數超過 maxRefillLoops → 中止並回報設定異常

步驟 2  抽籤
        以公共亂數（見第 4 章）在 drawable 上等機率選出 1 支籤 → 承辦股 U
        自籤筒移除該支籤

步驟 3  抵分扣減（依 offsetMap 對每個案類分別執行）
        對案類 X，應扣支數 = offsetMap[X]
            （UI 預設：offsetMap[本案類] = M − 1，其他案類 = 0）
        extra = offsetMap[X]
        while extra > 0 且籤筒 X 內尚有 U 的籤:
            自籤筒 X 移除 1 支 U 的籤
            extra = extra − 1
        if extra > 0:
            carryOverSkips[X][U] = carryOverSkips[X][U] + extra

步驟 4  補籤檢查（對本次被異動過的每一個籤筒各執行一次）
        loop（最多 config.rules.maxRefillLoops 次）:
            if 籤筒支數 <= 1
               或（籤筒支數 == 2 且該 2 支籤所屬股同屬一庭）:
                   執行「補籤」
                   繼續 loop
            else:
                   跳出 loop
        若達到迴圈上限仍未跳出 → 中止並回報設定異常

步驟 5  記錄
        產生 DRAW 紀錄並寫入歷史（見第 6 章）
```

### 3.4 補籤程序

```
補籤():
    1. 對每一個 active 的股，依其 ticketsPerCycle (N) 放入 N 支籤
    2. 立即扣除欠籤：
       對每一個 carryOverSkips[U] > 0 的股 U：
           d = min(carryOverSkips[U], 籤筒內 U 的籤數)
           自籤筒移除 d 支 U 的籤
           carryOverSkips[U] = carryOverSkips[U] − d
    3. cycle = cycle + 1
    4. 籤筒重新正規化排序
```

> 補籤後仍須回到步驟 4 的 loop 重新判斷，因為扣除欠籤可能使籤筒再度低於門檻，形成連鎖補籤。

### 3.5 規則要點確認清單

以下為需求釐清後確定的規則，實作不得偏離：

| # | 規則 |
|---|---|
| R-01 | 「抵 M 件」= 本案算 1 件 + 另扣 M−1 支籤，合計影響 M 件。 |
| R-02 | **先抵分扣減、後補籤檢查**。不得先補籤再扣，否則違反「籤筒中還有的籤優先抵分」。 |
| R-03 | 補籤門檻為絕對值，不隨股數或 N 值變動：剩 ≤ 1 支，或剩 2 支且同庭。 |
| R-04 | 「剩 2 支同庭」包含「剩 2 支同股」的情形。 |
| R-05 | 補籤檢查在所有扣減（抽出 + 抵分）完成後才執行，不在抽籤前執行。 |
| R-06 | 抵分範圍可依個別案件指定作用於哪些籤筒（預設僅本案類）。 |
| R-07 | 股別變動（新增、停用、修改 N）**立即生效**，直接異動現有籤筒，並於異動後執行一次補籤檢查。 |
| R-08 | 新股以「每輪 N 支籤」方式追分（例如 N = 3），追分結束後由管理者手動改回 N = 1。系統於管理頁常態顯示「N ≠ 1 的股」提醒。 |
| R-09 | 迴避股的籤**留在籤筒內**，僅本次不可被抽中，不影響籤筒支數與補籤判斷。 |
| R-10 | 籤筒與統計**不按年度重置**，連續運行；報表可自行選取期間。 |
| R-11 | 兩個案類的籤筒、輪次、欠籤完全獨立，僅在 offsetMap 明示指定時才互相影響。 |

### 3.6 R-02 / R-05 執行順序的推演範例（實作必讀）

本節以具體數字說明「先抵分、後補籤」為何不可對調。實作者與驗收者必須讀完本節。

範例中的組織設定：忠股、孝股屬**刑一庭**；仁股、愛股屬**刑二庭**；信股、義股屬**刑三庭**；和股、平股屬**刑四庭**。

- **順序甲（正確）**：抽出 → 抵分扣減 → 補籤檢查
- **順序乙（錯誤）**：抽出 → 補籤檢查 → 抵分扣減

#### 情境 A — 兩種順序結果相同（此為陷阱，非佐證）

> 籤筒 `[忠, 信]` 2 支｜抽中忠股｜抵 3 件（extra = 2）

| 步驟 | 順序甲 | 順序乙 |
|---|---|---|
| 抽出 | `[信]` 1 支 | `[信]` 1 支 |
| — | 抵分：筒內無忠籤 → 欠籤[忠]=2 | 補籤 → 9 支（忠1、信2、其餘各1） |
| — | 補籤 → 9 支，立即扣欠籤 1 支忠 → 8 支，欠籤[忠]=1 | 抵分：扣筒內 1 支忠 → 8 支，欠籤[忠]=1 |
| 結果 | **8 支，忠欠 1** | **8 支，忠欠 1** |

多數日常情況兩者結果相同，因為「補籤後立即扣欠籤」（3.4 步驟 2）事後把差異補平了。

**這正是本規則危險之處：順序寫錯在絕大多數情況下看不出來**，不會被日常操作察覺，直到踩到情境 B 或 C。因此本規則必須靠測試釘住，不能靠觀察。

#### 情境 B — 該補而未補，下一件案子退化為指定分案

> 籤筒 `[忠, 忠, 信]` 3 支｜抽中忠股｜抵 2 件（extra = 1）
> （忠股有 2 支籤屬正常狀態：上一輪剩 1 支忠籤時觸發補籤，忠股即有 2 支）

**順序甲（正確）**
```
1. 抽出          → [忠, 信]   2 支
2. 抵分 extra=1，筒內尚有忠籤 → 扣除
                 → [信]       1 支
3. 補籤檢查      1 支 ≤ 1 → 補籤
                 → [忠,孝,仁,愛,信,信,義,和,平]  9 支   ✓ 正常
```

**順序乙（錯誤）**
```
1. 抽出          → [忠, 信]   2 支
2. 補籤檢查      2 支，忠=刑一庭、信=刑三庭 → 不同庭 → 不補籤
3. 抵分 extra=1，扣除 1 支忠
                 → [信]       1 支   ✗ 流程結束，籤筒僅剩 1 支
```

**後果**：下一件同案類案件抽籤時，籤筒內僅有 1 支信股籤，**抽中信股機率為 100%**。

該次抽籤在形式上完備——drand 輪次存在、簽章驗證通過、雜湊鏈連續、驗證頁顯示「驗證通過」——但實質上是指定分案。外觀上唯一的異常是 `binBefore` 僅含 1 個元素。**這是本系統最嚴重的潛在瑕疵，因為它不會被任何既有的驗證機制攔下。**

#### 情境 C — 繞過「剩 2 支同庭」規則

> 籤筒 `[忠, 忠, 孝, 孝]` 4 支｜抽中忠股｜抵 3 件（extra = 2）
> （可達路徑：某輪剩 `[忠, 孝]` 同屬刑一庭 → 觸發補籤 → 10 支，忠孝各 2 支 → 其餘 6 支陸續抽出）

**順序甲（正確）**
```
1. 抽出          → [忠, 孝, 孝]   3 支
2. 抵分 extra=2：扣除 1 支忠（筒內僅存此 1 支）
                 → [孝, 孝]       2 支，extra=1 → 欠籤[忠]=1
3. 補籤檢查      2 支且孝、孝同屬刑一庭 → 補籤
                 → 10 支，立即扣欠籤 1 支忠 → 9 支   ✓ 正常
```

**順序乙（錯誤）**
```
1. 抽出          → [忠, 孝, 孝]   3 支
2. 補籤檢查      3 支 > 2 → 不補籤
3. 抵分 extra=2：扣除 1 支忠 → [孝, 孝] 2 支，欠籤[忠]=1
                 → 流程結束，停於 [孝, 孝]   ✗
```

**後果**：籤筒剩 `[孝, 孝]`，**接下來連續兩件案件必然皆為孝股**。

而 `[孝, 孝]` 這個狀態，正是 R-03「剩 2 支同一庭即補籤」所要防止的情形。順序錯置使該防護規則被完全繞過——檢查點在扣減之前執行完畢，扣減之後無人再行檢查。

#### 結構性結論

抵分只會**減少**籤筒支數，永不增加。因此：

> 在扣減之前執行補籤檢查，籤筒支數必然大於或等於扣減之後的支數。
> 故順序乙**只會「該補而未補」，不會「不該補而補」**。

失效方向為單向，且倒向危險的一側：籤筒趨於枯竭、隨機性下降，極端情況退化為指定分案。順序乙不會產生「多補籤」這類良性錯誤，故不得以「差異不大」為由放行。

由此亦可見 **R-02 與 R-05 為同一機制的兩種表述**：R-05 是機制（補籤檢查須在所有扣減完成後執行），R-02 是政策（先抵分後補籤）。實作上只要保證「所有扣減完成 → 才進入補籤檢查迴圈」，兩者同時滿足。

#### 稽核紀錄的差異

即使在情境 A 這類籤筒最終狀態相同的情況，兩種順序寫入 `history.jsonl` 的**紀錄內容仍不相同**：

| 欄位 | 順序甲（正確） | 順序乙（錯誤） |
|---|---|---|
| `offsetConsumedFromBin` | `{"jinsu": 0}` | `{"jinsu": 1}` |
| `offsetCarriedOver` | `{"jinsu": 2}` | `{"jinsu": 1}` |
| `refills[].cycleAfter` | 抵分歸屬於**補籤前的舊輪次** | 抵分歸屬於**補籤後的新輪次** |

日後查核「某股於第 N 輪被抵除幾支籤」時，兩者將給出不同答案。紀錄須附卷並供查核，此差異本身即構成問題。

#### 實作約束

下列為 `engine/lottery.mjs` 的硬性結構要求：

1. 抵分函式 `applyOffset()` **不得**於內部呼叫 `refill()`。補籤僅能由外層檢查迴圈觸發。
2. 補籤檢查必須為**迴圈**而非單次判斷（補籤後扣除欠籤可能使籤筒再度低於門檻）。
3. 單次抽籤僅開放一個公開入口，內部固定序列不可變更：
   `pick → removeDrawn → applyOffset(全部受影響案類) → refillLoop(全部受影響案類) → writeRecord`
4. 程式碼中須於此三處加上註解指向本節（`SPEC.md §3.6`），避免日後重構時被「優化」掉。

### 3.7 批次抽籤

- 一次可輸入多個案號（同一案類），系統**逐件依序**抽籤。
- 每件各自產生一筆獨立 DRAW 紀錄，共用一個 `batchId`。
- 前一件造成的籤筒異動（含補籤）**立即影響**下一件。
- 批次中任一件失敗（例如全數迴避）→ 中止整批，已完成的件不回復，並明確回報「已完成 k 件、第 k+1 件失敗」。
- 迴避與抵分設定可逐件指定；未指定者沿用批次預設值。

---

## 4. 隨機性與可驗證性

### 4.0 兩層驗證架構

drand 的簽章採 BLS12-381 曲線，驗證需要配對運算（pairing）。自行手寫約需一千行以上的密碼學程式碼，且任何細微錯誤都會使驗證形同虛設——**自己寫比用經過審計的現成實作更危險**。這與 §5.3「零第三方相依」直接衝突。

解法是把驗證拆成兩層，讓相依只出現在不影響分案作業的那一層：

| | 第一層 | 第二層 |
|---|---|---|
| **執行時機** | 每次抽籤當下 | 每日自動稽核、驗證頁 |
| **檔案** | `engine/drand.mjs` | `engine/bls.mjs` |
| **第三方相依** | **無** | `@noble/curves`（本身零相依、經專業審計） |
| **驗證內容** | ① 同一輪次向 ≥3 個獨立端點取值，回應須逐位元組完全一致<br>② `randomness` 須等於 `SHA256(signature)` | 以鏈公鑰對簽章做 BLS12-381 配對驗證 |
| **能主張的話** | 「多個獨立來源的值一致」 | 「經密碼學證明確為該鏈所簽」 |
| **失敗時** | **中止抽籤** | 記錄並告警，不影響已完成的分案 |

此設計的關鍵性質：**即使 `@noble/curves` 不可用或出現問題，抽籤仍可正常進行**，分案作業不會停擺，只是暫時少了第二層的密碼學證明。

`@noble/curves` 列為 `devDependencies` 且以確切版本釘選。抽籤路徑（`draw.yml`）不安裝、不載入它。

#### 實作上必須注意的一點

對於被竄改的簽章，`@noble/curves` 會**擲出例外**而非回傳 `false`（竄改後的位元組通常不是合法的曲線點）。若不攔截，稽核流程會直接崩潰——外觀上像是系統故障，而不是「偵測到竄改」。`engine/bls.mjs` 一律將例外轉為驗證失敗並附上原因。

### 4.1 亂數來源：drand 公共亂數燈塔

採用 drand（League of Entropy）公共隨機性燈塔。其特性符合法院抽籤的要求：

- 亂數由多個獨立機構的節點共同產生，**任何單一方無法操控**。
- 每一輪亂數在其產生時間之前**任何人都無法預知**。
- 每一輪亂數附有 BLS 門檻簽章，可用公開的鏈公鑰**離線驗證真偽**。
- 完全免費、無須註冊。

建議使用 quicknet 鏈（出塊週期約 3 秒）。實作前須依 drand 官方文件確認鏈雜湊、公鑰與 API 路徑，並將其寫入 `config.json` 的 `drand` 區塊，不得寫死在程式中。

備援端點須設定 3 個以上（例如 api.drand.sh / api2.drand.sh / api3.drand.sh / drand.cloudflare.com），依序重試。

### 4.2 兩階段「承諾—開籤」流程

為證明「目標亂數在請求送出時尚不存在」，抽籤分兩個階段，各產生一次 git commit：

**階段一：承諾（COMMIT）**

1. 讀取 drand 目前輪次 `R_now`。
2. 計算目標輪次 `R_target = R_now + 2`（約 6 秒後才會產生，此刻無人能預知其值）。
3. 產生 `commitPayload`，內容包含：
   - 批次 ID、案類、全部案號（依輸入順序）
   - 各件的抵分設定、迴避股清單
   - 抽籤前籤筒快照 `binBefore` 與其雜湊
   - `R_target`、drand 鏈雜湊
   - 操作者帳號、請求時間
4. 將 `commitPayload` 以一次 commit 寫入 `data/pending/{batchId}.json` 並推送。**此步驟完成後，抽籤標的即不可更改。**

**階段二：開籤（REVEAL）**

5. 輪詢 drand 直到 `R_target` 可取得，取回 `randomness` 與 `signature`。
6. 驗證簽章是否符合鏈公鑰；驗證失敗 → 中止。
7. 以 `randomness` 推導各件的抽籤位置（見 4.3）。
8. 執行第 3 章演算法，寫入 DRAW 紀錄、更新 `state.json`、刪除 pending 檔，一次 commit 推送。

### 4.3 由亂數推導抽籤位置

必須使用**拒絕採樣**，不得直接取模，否則會產生模數偏差（低位索引機率略高），影響公平性。

```
seed = HMAC-SHA256(key = drand.randomness,
                   msg = commitPayloadHash)

pick(n, itemSeq):                       # n = drawable 的支數
    counter = 0
    loop:
        block = HMAC-SHA256(key = seed,
                            msg = "pick|" + itemSeq + "|" + counter)
        v     = block 前 8 bytes 視為 big-endian 無號 64 位元整數
        limit = floor(2^64 / n) * n
        if v < limit:
            return v mod n              # 即 drawable 陣列的索引
        counter = counter + 1
```

- `itemSeq` 為該件在批次中的序號（自 0 起算），使一輪亂數可安全推導出多件結果。
- 抵分與補籤過程**不使用亂數**，皆為確定性運算。

### 4.4 驗證頁

系統提供 `verify.html`，任何人輸入紀錄編號後，前端會：

1. 自 GitHub raw 取得該筆 DRAW 紀錄與 `commitPayload`。
2. 重算 `commitPayloadHash`，比對。
3. 自 drand 公開端點取得該輪次亂數，比對紀錄中的值，並驗證 BLS 簽章。
4. 重算 `seed` 與 `pickIndex`，比對 `binBefore[pickIndex]` 是否等於紀錄中的承辦股。
5. 驗證該筆紀錄的 `prevRecordHash` 是否等於前一筆的 `recordHash`（雜湊鏈連續）。

全部通過 → 大字顯示「✓ 本筆抽籤結果驗證通過」，並列出各項比對明細。

同時提供一支獨立的離線驗證腳本 `tools/verify-all.mjs`，可一次驗證全部歷史，供稽核使用。

### 4.5 亂數不可用時的處理

**drand 全部端點皆不可用時，必須中止抽籤並明確回報錯誤。嚴禁自動改用本機亂數。** 若需緊急分案，改走第 12 章離線備援程序。

---

## 5. 系統架構

### 5.1 整體架構

```
                    GitHub Repository（公開）
                    ── 唯一真實來源，全部變更皆為 git commit ──
   data/config.json      組織與規則設定
   data/state.json       兩個籤筒的即時狀態
   data/history.jsonl    抽籤與稽核紀錄（只增不刪，雜湊鏈保護）
   data/operators.json   抽籤授權清單
   data/pending/         承諾階段暫存
              │                                    ▲
     公開讀取 │（免登入）              唯一寫入者   │
              ▼                                    │
   ┌──────────────────────┐        ┌───────────────────────────────┐
   │ GitHub Pages 公開網站 │        │ GitHub Actions 抽籤引擎        │
   │  index.html  公開看板 │        │  ・比對授權清單                │
   │  history.html 歷史查詢│        │  ・取 drand 公共亂數           │
   │  verify.html  結果驗證│        │  ・執行抽籤演算法              │
   │  admin.html   組織管理│        │  ・寫入紀錄並 commit           │
   │  draw.html    抽籤台  │        │  ・觸發通知                    │
   └──────────────────────┘        └───────────────────────────────┘
              │                                    ▲
              │ 大字體介面                 觸發     │
              └────────────────────────────────────┘
                       授權者以個人權杖發動

   ┌──────────────┐   ┌──────────────┐   ┌──────────────────┐
   │ 網址公開分享 │   │ LINE 群組推播 │   │ 本機同步腳本      │
   │  公開看板網址 │   │ Messaging API │   │ sync-local.ps1   │
   └──────────────┘   └──────────────┘   └──────────────────┘
```

### 5.2 為何不採用「線上共享記事本」

| 面向 | 共享記事本 | 本方案 |
|---|---|---|
| 誰能修改 | 知道網址的人都能改 | 只有 GitHub Actions 能寫入資料檔 |
| 竄改痕跡 | 無，改了看不出來 | 每次變更皆為 git commit，且有雜湊鏈 |
| 亂數公信力 | 由操作者的電腦產生，無法自證 | 公共亂數燈塔，第三人可重算驗證 |
| 重抽到滿意為止 | 可能，且無紀錄 | 承諾階段先上鏈，重抽必留痕跡 |
| 權限分級 | 只有「能改」與「不能改」 | 讀 / 抽籤 / 管理三級，可個別撤銷 |
| 成本 | 免費 | 免費（公開 repo 的 Actions 與 Pages 皆免費） |

### 5.3 技術選型

| 項目 | 選擇 | 理由 |
|---|---|---|
| 抽籤引擎 | Node.js 20+，僅用內建模組（`node:crypto`、`node:test`） | 零第三方相依 → 無供應鏈風險，十年後仍可執行 |
| BLS 驗簽（唯一例外） | `@noble/curves`，僅用於稽核與驗證路徑，抽籤路徑不載入 | 配對密碼學自行實作風險更高。範圍限縮見 §4.0 |
| 前端 | 原生 HTML / CSS / JavaScript，無建置流程 | 無 build step，任何人可直接讀原始碼稽核；長期可維護 |
| 資料格式 | JSON + JSONL（UTF-8，無 BOM，LF 換行） | 純文字，git diff 可讀，永不鎖定廠商 |
| 雜湊 | SHA-256；JSON 正規化採 RFC 8785 (JCS) | 確保任何實作重算結果一致 |
| 託管 | GitHub Pages + GitHub Actions | 公開 repo 完全免費 |

---

## 6. 資料模型

### 6.1 `data/config.json`

```json
{
  "schemaVersion": 1,
  "updatedAt": "2026-08-04T09:00:00+08:00",

  "courts": [
    { "id": "ct-01", "name": "刑事第一庭", "shortName": "刑一", "order": 1, "active": true, "color": "blue" },
    { "id": "ct-02", "name": "刑事第二庭", "shortName": "刑二", "order": 2, "active": true, "color": "green" },
    { "id": "ct-03", "name": "刑事第三庭", "shortName": "刑三", "order": 3, "active": true, "color": "amber" },
    { "id": "ct-04", "name": "刑事第四庭", "shortName": "刑四", "order": 4, "active": true, "color": "purple" }
  ],

  "units": [
    { "id": "un-01", "name": "忠股", "courtId": "ct-01", "order": 1, "active": true, "ticketsPerCycle": 1, "note": "" },
    { "id": "un-02", "name": "孝股", "courtId": "ct-01", "order": 2, "active": true, "ticketsPerCycle": 1, "note": "" },
    { "id": "un-03", "name": "仁股", "courtId": "ct-02", "order": 3, "active": true, "ticketsPerCycle": 1, "note": "" },
    { "id": "un-04", "name": "愛股", "courtId": "ct-02", "order": 4, "active": true, "ticketsPerCycle": 1, "note": "" },
    { "id": "un-05", "name": "信股", "courtId": "ct-03", "order": 5, "active": true, "ticketsPerCycle": 1, "note": "" },
    { "id": "un-06", "name": "義股", "courtId": "ct-03", "order": 6, "active": true, "ticketsPerCycle": 1, "note": "" },
    { "id": "un-07", "name": "和股", "courtId": "ct-04", "order": 7, "active": true, "ticketsPerCycle": 1, "note": "" },
    { "id": "un-08", "name": "平股", "courtId": "ct-04", "order": 8, "active": true, "ticketsPerCycle": 1, "note": "" }
  ],

  "caseTypes": [
    { "id": "jinsu",      "name": "金訴",   "order": 1, "active": true },
    { "id": "jinzhongsu", "name": "金重訴", "order": 2, "active": true }
  ],

  "rules": {
    "refillWhenRemainingAtMost": 1,
    "refillWhenTwoTicketsSameCourt": true,
    "defaultOffsetScope": "sameCaseType",
    "maxOffsetPerCase": 10,
    "maxRefillLoops": 20,
    "redrawReturnsTicket": true
  },

  "drand": {
    "chainHash": "（實作時填入 quicknet 鏈雜湊）",
    "publicKey": "（實作時填入鏈公鑰）",
    "periodSeconds": 3,
    "roundOffset": 2,
    "endpoints": [
      "https://api.drand.sh",
      "https://api2.drand.sh",
      "https://api3.drand.sh",
      "https://drand.cloudflare.com"
    ]
  },

  "ui": {
    "baseFontPx": 28,
    "resultFontPx": 128,
    "animation": false,
    "highContrast": true
  },

  "notify": {
    "web":  { "enabled": true },
    "line": { "enabled": true, "includeCaseNo": true, "groupIds": ["（Secret 管理，此處僅記錄啟用狀態）"] }
  },

  "privacy": {
    "forbiddenFieldHints": ["當事人姓名", "身分證字號", "出生年月日", "住址", "電話"],
    "warnOnPatterns": ["[A-Z][12]\\d{8}"]
  }
}
```

### 6.2 `data/state.json`

```json
{
  "schemaVersion": 1,
  "seq": 128,
  "updatedAt": "2026-08-04T09:15:33+08:00",
  "bins": {
    "jinsu": {
      "tickets": ["un-01", "un-03", "un-03", "un-05", "un-08"],
      "cycle": 17,
      "carryOverSkips": { "un-02": 1 },
      "lastRecordId": "R-000128"
    },
    "jinzhongsu": {
      "tickets": ["un-02", "un-04", "un-06", "un-07"],
      "cycle": 9,
      "carryOverSkips": {},
      "lastRecordId": "R-000121"
    }
  },
  "prevStateHash": "sha256:…",
  "stateHash": "sha256:…"
}
```

- `seq` 為跨案類的全域遞增序號。
- `tickets` 恆為 3.2 節定義的正規化排序。
- `stateHash` = SHA-256(RFC 8785 正規化後的本物件，排除 `stateHash` 欄位本身)。

### 6.3 `data/history.jsonl`

每行一筆 JSON 紀錄，只增不刪。共同欄位：

| 欄位 | 說明 |
|---|---|
| `recordId` | `R-000128`，六位補零，全域唯一遞增 |
| `type` | 紀錄類型，見 6.4 |
| `seq` | 全域序號 |
| `at` | ISO 8601 含時區（+08:00） |
| `operator` | 執行者 GitHub 帳號 |
| `workflowRunUrl` | 對應的 Actions 執行紀錄網址（公開可查） |
| `prevRecordHash` | 前一筆的 `recordHash`，形成雜湊鏈 |
| `recordHash` | SHA-256(本筆正規化 JSON，排除 `recordHash`) |

### 6.4 紀錄類型

| 類型 | 用途 | 是否改變籤筒 |
|---|---|---|
| `DRAW` | 正常抽籤 | 是 |
| `REDRAW` | 抽後迴避而重抽 | 是 |
| `AMEND` | 更正**不影響籤筒**的欄位（案號、備註等） | 否 |
| `OFFSET_AMEND` | 更正抵分數，僅調整籤筒與欠籤，**承辦股不變** | 是 |
| `VOID` | 作廢整筆抽籤並回復籤筒 | 是 |
| `CONFIG_CHANGE` | 庭 / 股 / 案類 / 規則設定變更 | 否 |
| `BIN_ADJUST` | 因股別立即生效變動所致的籤筒異動 | 是 |
| `OFFLINE_BACKFILL` | 離線備援抽籤的事後補登 | 是 |
| `DENIED` | 未授權者嘗試發動抽籤的拒絕紀錄 | 否 |

### 6.5 `DRAW` 紀錄範例

```json
{
  "recordId": "R-000128",
  "type": "DRAW",
  "seq": 128,
  "at": "2026-08-04T09:15:33+08:00",
  "operator": "github:clerk-wang",
  "workflowRunUrl": "https://github.com/OWNER/court-lottery/actions/runs/1234567890",

  "batchId": "B-000042",
  "itemSeq": 0,
  "caseTypeId": "jinsu",
  "caseTypeName": "金訴",
  "caseNo": "115年度金訴字第123號",
  "note": "",

  "offsetCount": 3,
  "offsetMap": { "jinsu": 2 },
  "excludedUnitIds": ["un-04"],
  "excludeReason": "承辦法官自行迴避",

  "binBefore": ["un-01","un-02","un-03","un-04","un-05","un-06","un-07","un-08"],
  "binBeforeHash": "sha256:…",
  "drawableCount": 7,

  "commitPayloadHash": "sha256:…",
  "drand": {
    "chainHash": "…",
    "round": 5123456,
    "randomness": "…",
    "signature": "…",
    "signatureVerified": true
  },
  "pickIndex": 4,

  "resultUnitId": "un-05",
  "resultUnitName": "信股",
  "resultCourtId": "ct-03",
  "resultCourtName": "刑事第三庭",

  "offsetConsumedFromBin": { "jinsu": 2 },
  "offsetCarriedOver": { "jinsu": 0 },
  "refills": [
    { "binId": "jinsu", "reason": "remaining<=1", "added": 8,
      "carryOverApplied": { "un-02": 1 }, "cycleAfter": 17 }
  ],

  "binAfter": ["un-01","un-03","un-03","un-05","un-08"],
  "binAfterHash": "sha256:…",

  "prevRecordHash": "sha256:…",
  "recordHash": "sha256:…"
}
```

### 6.6 `data/operators.json`

```json
{
  "schemaVersion": 1,
  "operators": [
    { "githubLogin": "clerk-wang", "displayName": "王書記官", "role": "DRAW_OPERATOR",
      "validFrom": "2026-08-01", "validTo": null, "note": "主要操作者" },
    { "githubLogin": "clerk-lee",  "displayName": "李書記官", "role": "DRAW_OPERATOR",
      "validFrom": "2026-08-01", "validTo": null, "note": "備援操作者" },
    { "githubLogin": "chief-chen", "displayName": "陳庭長",   "role": "ADMIN",
      "validFrom": "2026-08-01", "validTo": null, "note": "組織設定管理" }
  ]
}
```

撤銷授權 = 將 `validTo` 填為當日日期（保留紀錄），並要求該員自行刪除其個人權杖。

---

## 7. 更正、作廢與重抽

三種機制界線分明，操作者不得誤用。抽籤台與管理頁須以文字明確引導。

### 7.1 決策表

| 發生的狀況 | 應使用 | 承辦股是否改變 | 籤筒是否改變 |
|---|---|---|---|
| 案號打錯、備註寫錯 | `AMEND` | 否 | 否 |
| 抵分數填錯（承辦股正確） | `OFFSET_AMEND` | 否 | 是（補扣或退還籤） |
| 抽錯案類、整筆不應存在 | `VOID` | 該筆作廢 | 是（完全回復） |
| 抽中後才發現該股應迴避 | `REDRAW` | 是 | 是 |

### 7.2 `AMEND`（更正）

- 新增一筆 `AMEND` 紀錄，含 `targetRecordId`、`changes`（欄位舊值 / 新值）、`reason`（必填）。
- **原紀錄完全不動**。公開看板與歷史頁在原紀錄旁顯示「已更正 →」連結，兩筆都看得到。
- 可更正欄位僅限：`caseNo`、`note`、`excludeReason`。

### 7.3 `OFFSET_AMEND`（抵分更正）

- 僅調整抵分後果，承辦股與抽籤結果不變。
- 抵分數**增加**：對差額執行 3.3 步驟 3 的扣減邏輯（籤筒優先、不足記欠籤），再跑補籤檢查。
- 抵分數**減少**：先沖銷欠籤，仍有餘額則將對應支數的籤退還籤筒。退還後**不執行**補籤檢查（籤數只會增加）。
- 必填 `reason`，並記錄調整前後的籤筒快照。

### 7.4 `VOID`（作廢並回復籤筒）

- **限制**：僅能作廢該籤筒**最近一筆**且其後該籤筒未再發生任何狀態變動的紀錄。若已有後續抽籤，系統拒絕並提示「請逐筆由最新往回作廢」。
- 回復方式：將 `state.bins[X]` 完整還原為該筆紀錄的 `binBefore`、`carryOverSkips` 與 `cycle`。
- 必填 `reason`，需 `ADMIN` 角色。
- 原 `DRAW` 紀錄保留，標示為「已作廢」，並以刪除線與紅色標籤呈現。

### 7.5 `REDRAW`（抽後迴避重抽）

- 適用於抽出後才發現承辦股應迴避。
- 流程：
  1. 產生 `REDRAW` 紀錄，含 `originalRecordId`、`recusedUnitId`、`recuseReason`（必填）。
  2. 依 `rules.redrawReturnsTicket` 決定原籤是否放回籤筒：
     - `true`（**預設**）：放回。理由是該股並未實際受分案，應回復其受分機會。
     - `false`：不放回，該股本輪視為已輪畢。
  3. 撤銷原次抽籤的抵分後果（比照 7.3 減少邏輯）。
  4. 以**新的一次兩階段承諾—開籤流程**重抽，`excludedUnitIds` 加入該迴避股。
- `REDRAW` 全程公開，且在公開看板明確標示為重抽案件。

---

## 8. 權限與安全

### 8.1 角色

| 角色 | 可做的事 | 不能做的事 |
|---|---|---|
| **公眾**（免登入） | 讀取全部設定、籤筒狀態、歷史紀錄、驗證結果 | 任何寫入 |
| **DRAW_OPERATOR** | 發動抽籤、批次抽籤、REDRAW、AMEND、OFFSET_AMEND | 修改組織設定、VOID、修改授權清單、直接修改任何資料檔 |
| **ADMIN** | 上列全部 + 組織設定、VOID、授權清單維護 | 直接推送資料檔（仍須經 Actions） |
| **Repo Owner** | 帳號層級的最終控制 | — |

### 8.2 「可授權但不直接分享」的實作

這是本系統的核心安全設計，共四層：

**第 1 層 — 每人一組個人權杖**
每位授權者用自己的 GitHub 帳號建立 **fine-grained personal access token**：
- Repository access：僅限本 repo
- Permissions：**只勾選 `Actions: Read and write` 一項**
- 有效期限：建議 90 天，到期須重新申請

因為沒有 `Contents: write`，該權杖**在技術上完全無法修改任何資料檔**，只能發動抽籤流程。權杖存於該操作者自己電腦的瀏覽器 localStorage，不經任何第三方伺服器。

**第 2 層 — 分支保護規則（Ruleset）**

對 `main` 分支設定：
- 禁止 force push（`Block force pushes`）
- 禁止刪除分支（`Restrict deletions`）

⚠ **不可啟用 `Restrict updates`。** GitHub 官方文件所列可加入 bypass list 的對象僅有
repository admin／organization owner、maintain 或 write 角色、team、GitHub App、Dependabot，
**`github-actions[bot]` 與 `GITHUB_TOKEN` 不在其中**。啟用後被擋住的會是抽籤工作流程本身。

若要達成「只有工作流程能寫入資料檔」，需改用下列其中一種，並同時啟用 `Restrict updates`：
- **Deploy key**：產生 SSH 金鑰對，公鑰設為 repo 的 write deploy key、私鑰存為 Actions Secret，
  將該 deploy key 加入 bypass list，工作流程改以 SSH 推送。
- **專屬 GitHub App**：安裝於本 repo 並加入 bypass list，工作流程以 App token 推送。

### 8.2.1 單人持有個人帳號時的重大限制

上述四層防護是以「repo 擁有者 ≠ 抽籤操作者」為前提設計的。
**若 repo 建於個人帳號且由同一人持有並操作，第 1～3 層對該擁有者全部失效**：

| 層 | 對第三人 | 對擁有者本人 |
|---|---|---|
| 1. 權杖僅具 Actions 權限 | 有效 | 無效（可自本機直接推送） |
| 2. 分支保護 | 有效 | **無效（擁有者即規則的管理者，可自行 bypass 或停用）** |
| 3. `operators.json` 允許清單 | 有效 | 無效（可自行修改） |
| 4. 公開且可驗證的軌跡 | 有效 | **有效** |

因此在單人架構下，真正的保證來自**第 4 層與雜湊鏈、每日稽核**——這些不論由誰推送都成立：
任何對 `data/` 的手動修改都會使雜湊鏈斷裂、稽核失敗並自動開 issue 告警。

**但第 4 層要真正生效，repo 必須公開**，使外部第三人握有可比對的副本；
否則「歷史被改寫」將無人能在外部察覺。這使得 §15 的 A-04（案號公開與否）
不只是隱私取捨，而是**整套信任模型能否成立的前提**。

若日後需要制度上的權責分離，應將 repo 移至 Organization，並使抽籤操作者不具 owner 身分。

**第 3 層 — 允許清單比對**
每個 workflow 的第一個步驟即比對 `github.actor` 是否存在於 `data/operators.json` 且在有效期內、角色足夠。不符合者立即中止，並寫入一筆公開的 `DENIED` 紀錄。撤銷某人權限只需修改此清單，**不需要更換任何共用密碼，也不影響其他人**。

**第 4 層 — 全程公開留痕**
每一次抽籤請求（含被拒絕者）都對應一次 GitHub Actions 執行紀錄，公開可查，含發動者帳號與時間。承諾階段的 commit 使「先偷看結果再決定要不要送出」在技術上不可行。

### 8.3 其他安全要求

- Repo Owner 與所有 ADMIN 帳號**必須啟用雙因素驗證**，建議使用 passkey。
- LINE Channel Access Token 等機密僅存於 GitHub Actions Secrets，嚴禁寫入 repo。
- Workflow 使用 `concurrency: group: lottery, cancel-in-progress: false`，避免並行抽籤造成籤筒狀態競爭。
- 所有 Action 相依套件以 commit SHA 釘選版本，不使用浮動 tag。
- 每筆抽籤前後皆重新驗證 `state.json` 的 `stateHash` 與歷史雜湊鏈；不一致即中止並告警。

### 8.4 個資保護（重要）

因 repo 為公開，**嚴禁在任何欄位填入當事人姓名、身分證字號、出生年月日、住址、電話等個人資料**。

- 抽籤台輸入框於送出前執行前端檢查，命中 `config.privacy.warnOnPatterns`（如身分證字號格式）時以大字警示並阻擋送出。
- Workflow 端再執行一次相同檢查，作為第二道防線。
- 上線前應確認案號公開之妥適性（見第 15 章）。

---

## 9. 使用者介面

### 9.1 共通的大字體與無障礙規範

| 項目 | 規格 |
|---|---|
| 基礎字級 | 28 px（`html { font-size: 28px }`，以 rem 排版） |
| 抽籤結果字級 | `clamp(72px, 12vw, 160px)`，字重 700 |
| 內文最小字級 | 22 px。**全站不得出現小於 20 px 的文字** |
| 文字對比 | ≥ 7:1（WCAG 2.2 AAA） |
| 按鈕尺寸 | 最小 64 × 64 px，按鈕間距 ≥ 16 px |
| 字體 | Noto Sans TC / 思源黑體，字重 ≥ 500；不使用細體與斜體 |
| 行高 | 1.7 |
| 顏色使用 | **不得單以顏色表意**。庭別以「顏色 + 圖示 + 文字」三重標示，色盲亦可辨識 |
| 縮放 | 瀏覽器縮放至 200% 不破版 |
| 鍵盤 | 全站可純鍵盤操作，焦點框 3 px 高對比外框 |
| 動畫 | **無**（已確認）。結果按下即顯示 |
| 語言 | 繁體中文，`lang="zh-Hant-TW"` |
| 深色模式 | 支援 `prefers-color-scheme`，兩種模式皆須達 AAA 對比 |

### 9.2 頁面清單

#### `index.html` — 公開看板（首頁）

由上而下：

1. **最新結果區**（占首屏）
   超大字顯示：案號 → 承辦股 → 所屬庭 → 抽籤時間。若為重抽或已作廢，加大紅色標籤。
2. **籤筒現況區**（左右並列兩個案類）
   每個股一張大卡片，依庭別分色分區：
   - 股名（大字）＋ 庭別標籤
   - 剩餘籤數以**籤條圖示**視覺化（1 支畫 1 條，一目了然）
   - 剩餘 0 支者卡片轉灰階並顯示「本輪已輪畢 ✓」
   - 有欠籤者顯示紅色「欠 N 支」
   - 每輪籤數 N ≠ 1 者顯示藍色「每輪 N 支」提示
   - 籤筒下方顯示：目前輪次、籤筒總支數、下次補籤門檻
3. **最近 10 筆紀錄**（大字表格）
4. **分享區**：網址、複製網址按鈕、訂閱網址

#### `history.html` — 歷史查詢

- 大字表格，欄位：日期、案類、案號、承辦股、庭、抵分、狀態、驗證。
- 篩選：案類、期間、股別、紀錄類型。
- 每列有「驗證」按鈕直連 `verify.html`。
- 「匯出 CSV」按鈕（UTF-8 with BOM，Excel 可直接開啟）。
- 統計摘要：各股累計受分件數、累計抵分件數、實際件數／期望件數比值（公平性檢視）。
- 統計可自選期間（因籤筒不按年度重置，統計以期間篩選呈現）。

#### `draw.html` — 抽籤台（僅授權者使用）

分步驟引導，一次只呈現一件事，每步都是超大字與大按鈕：

```
步驟 0  首次使用：貼上個人權杖 → 存於本機瀏覽器（顯示「已授權：〇〇〇」）
步驟 1  選案類          [ 金 訴 ]  [ 金 重 訴 ]      ← 兩個超大按鈕
步驟 2  輸入案號        單筆或多行貼上（批次）
步驟 3  抵分設定        [ − ]  3 件  [ + ]          ← 大型加減按鈕
                        抵分範圍：☑金訴  ☐金重訴（預設僅本案類）
步驟 4  迴避設定        勾選本次應迴避的股（預設全不勾）
步驟 5  確認頁          超大字覆誦全部輸入內容，紅色「確認抽籤」按鈕
步驟 6  執行中          「抽籤進行中，請稍候…」＋ 進度說明
步驟 7  結果            超大字顯示承辦股，另有［列印紀錄表］［回首頁］
```

- 步驟 5 的確認頁不可略過，防止誤觸。
- 批次抽籤在步驟 7 以大字清單逐件列出結果。
- 列印版面：A4 直式，含案號、承辦股、抽籤時間、drand 輪次、紀錄編號與驗證網址，供附卷。

#### `verify.html` — 結果驗證

輸入紀錄編號，逐項顯示驗證明細與最終大字結論（見 4.4）。

#### `admin.html` — 組織管理（僅 ADMIN）

- 庭：新增、更名、停用、排序
- 股：新增、更名、改隸屬庭、停用、設定每輪籤數 N
- 案類：新增、更名、停用
- 規則參數調整
- 操作者授權清單維護
- 所有變更皆須填理由，經確認頁，並產生公開的 `CONFIG_CHANGE` 紀錄
- 常態顯示紅色提醒：「目前有 N 個股的每輪籤數不等於 1」

---

## 10. 分享與通知

### 10.1 公開網頁

> **已移除 QR code 功能。** 曾以自行實作的編碼器產生 QR code（因不得對外部服務
> 發出請求，見 §5.3），該實作通過了 Reed-Solomon 碼字合法性、BCH 格式資訊、
> 反向讀取一致性等數學驗證，但**實機掃描無法辨識**——代表仍有這些測試涵蓋不到的
> 錯誤，只有真正的解碼器才驗得出來。
>
> 由於本系統的每一項功能都必須能被實際驗證，無法確認正確的功能不應留在系統中，
> 故整項移除。分享改以網址與「複製網址」按鈕達成。
> 日後若要恢復，必須先具備可自動化的真實解碼驗證，不能僅靠自我一致的測試。

- 公開看板網址為 `https://{owner}.github.io/court-lottery/`，免登入、免帳號，手機平板皆可讀。
- 首頁顯示完整網址並提供「複製網址」按鈕，可列印張貼於公布欄或傳送至群組。
- 提供「複製網址」按鈕。
- 每筆紀錄有專屬固定網址 `#/record/R-000128`，可直接引用。

### 10.2 LINE 群組推播

**技術說明**：LINE Notify 服務已於 2025 年 3 月 31 日終止，本系統採用 **LINE Messaging API（LINE 官方帳號）**。

設定步驟（一次性）：
1. 申請 LINE 官方帳號。
2. 於 LINE Developers 建立 Messaging API channel，取得 Channel Access Token。
3. 將 Token 存入 GitHub Actions Secret `LINE_CHANNEL_TOKEN`。
4. 將官方帳號加入目標群組，透過 webhook 取得 `groupId`，存入 Secret `LINE_GROUP_IDS`。
5. 於 `config.json` 開啟 `notify.line.enabled`。

推播內容（可設定是否含案號）：
```
【分案抽籤結果】
案類：金訴
案號：115年度金訴字第123號
承辦：刑事第三庭 信股
時間：2026-08-04 09:15
抵分：3 件
查詢與驗證：https://…/#/record/R-000128
```

限制與注意：
- 免費方案有每月訊息量上限，實際額度以 LINE 官方公告為準。批次抽籤合併為一則訊息發送以節省額度。
- 額度不足或推播失敗**不得中止抽籤**，僅記錄推播失敗事件並在看板顯示警示。
- 若評估案號不宜外流，可將 `notify.line.includeCaseNo` 設為 `false`，僅推播「已完成 N 件抽籤，請至看板查閱」。

### 10.3 其他訂閱管道（免額外設定）

- **Atom 訂閱**：GitHub 原生提供 `.../commits/main.atom`，任何人以閱讀器即可追蹤，免帳號。
- **Email**：有 GitHub 帳號者可對 repo 設定 Watch，即自動收信。

### 10.4 本機歷史同步

提供 `tools/sync-local.ps1`（Windows PowerShell）：

1. 下載線上 `history.jsonl`、`config.json`、`state.json`。
2. **驗證雜湊鏈完整性**，不通過即以紅色大字警告並中止。
3. 輸出至本機指定資料夾：
   - `抽籤紀錄_YYYYMMDD.csv`（UTF-8 with BOM，Excel 直接開）
   - `抽籤紀錄_YYYYMMDD.jsonl`（原始備份）
   - `抽籤紀錄_YYYYMMDD.html`（大字可列印版）
4. 保留每次同步的獨立檔案，形成本機多重備份。
5. 可掛入 Windows 工作排程器每日自動執行。

另提供 `tools/verify-all.mjs`，離線驗證全部歷史紀錄的雜湊鏈與 drand 結果，供內部稽核或外部查核使用。

---

## 11. GitHub Actions 工作流程

### 11.1 `draw.yml` — 抽籤

- 觸發：`workflow_dispatch`
- 輸入：`caseTypeId`、`caseNos`（多行）、`offsetCount`、`offsetMap`（JSON）、`excludedUnitIds`、`note`
- `concurrency: { group: lottery, cancel-in-progress: false }`
- 步驟：
  1. 比對 `github.actor` 與 `data/operators.json`；不符 → 寫 `DENIED` 紀錄後失敗結束
  2. 個資樣式檢查
  3. 驗證 `state.json` 雜湊與歷史雜湊鏈
  4. 取 drand `R_now`，計算 `R_target`
  5. 產生 `commitPayload`，commit 至 `data/pending/`（**承諾階段**）
  6. 等待並取得 `R_target` 亂數，驗證 BLS 簽章
  7. 執行抽籤引擎（第 3 章演算法）
  8. 寫入 `history.jsonl`、更新 `state.json`、刪除 pending，commit（**開籤階段**）
  9. 觸發 LINE 推播
  10. 在 Actions 摘要頁以大字輸出結果

### 11.2 其他工作流程

| 檔案 | 用途 | 觸發 | 需要角色 |
|---|---|---|---|
| `redraw.yml` | 抽後迴避重抽 | `workflow_dispatch` | DRAW_OPERATOR |
| `amend.yml` | AMEND / OFFSET_AMEND | `workflow_dispatch` | DRAW_OPERATOR |
| `void.yml` | 作廢並回復籤筒 | `workflow_dispatch` | ADMIN |
| `config.yml` | 組織設定變更、股別立即生效異動 | `workflow_dispatch` | ADMIN |
| `backfill.yml` | 離線備援結果補登 | `workflow_dispatch` | ADMIN |
| `verify.yml` | 每日自動全量驗證雜湊鏈與 drand，異常時開 issue 告警 | `schedule` 每日 | — |
| `pages.yml` | 建置並發布 GitHub Pages | push to main | — |

### 11.3 中斷回復

若流程在「承諾」之後、「開籤」之前中斷（網路中斷、Actions 逾時），`data/pending/` 會殘留檔案。

- 公開看板偵測到 pending 檔案時，顯示大字警示「有一批抽籤尚未完成」。
- 提供 `resume.yml`：以原 `commitPayload` 中已固定的 `R_target` 繼續開籤。因目標輪次早已上鏈固定，**續行不會產生任何操控空間**。
- 若因故不再續行，須由 ADMIN 執行「放棄」並寫入公開紀錄說明原因。

---

## 12. 離線備援程序

當 GitHub 或 drand 不可用而必須立即分案時：

1. 由 ADMIN 自公開看板列印當下的籤筒狀態（含每支籤的所屬股），並經二人以上簽名確認。
2. 以實體籤筒依相同規則人工抽籤，全程二人以上在場見證。
3. 服務恢復後，ADMIN 以 `backfill.yml` 補登，紀錄類型為 `OFFLINE_BACKFILL`，必填：
   - 抽籤實際時間、在場見證人、人工抽籤結果
   - 抽籤前籤筒列印本的掃描檔連結
4. 系統依補登結果套用第 3 章的抵分與補籤邏輯，使籤筒狀態同步。
5. 公開看板永久標示該筆為「離線備援抽籤」（無 drand 驗證資料）。

---

## 13. 擴充性

系統於設計上保留下列擴充空間，不需修改程式碼即可調整：

| 擴充項目 | 作法 |
|---|---|
| 增加庭 | `config.json` 新增 `courts` 項目 |
| 增加股 | 新增 `units` 項目，設定 `courtId` 與 `ticketsPerCycle` |
| 增加案類 | 新增 `caseTypes` 項目，系統自動建立獨立籤筒 |
| 調整補籤門檻 | 修改 `rules.refillWhenRemainingAtMost` |
| 關閉「2 支同庭」規則 | `rules.refillWhenTwoTicketsSameCourt = false` |
| 調整重抽時原籤是否放回 | `rules.redrawReturnsTicket` |
| 更換亂數燈塔 | 修改 `config.drand`（鏈雜湊、公鑰、端點） |

程式中**不得**寫死 8 股、4 庭、2 案類等任何數字，一律自 `config.json` 讀取。

---

## 14. 測試需求

抽籤引擎須有完整單元測試，且以固定亂數種子確保可重現。下列情境為**驗收必測項目**：

**基本流程**
1. 初始 8 支籤，連續抽 7 次後剩 1 支 → 觸發補籤 → 籤筒為 9 支，原剩餘股有 2 支
2. 剩 2 支且分屬不同庭 → **不**補籤
3. 剩 2 支且同屬一庭 → 補籤 → 籤筒為 10 支
4. 剩 2 支且為同一股的兩支籤 → 補籤（R-04）

**抵分**
5. 抵 3 件，籤筒內該股尚有 ≥ 2 支 → 全數自籤筒扣除，欠籤為 0
6. 抵 3 件，籤筒內該股僅剩 1 支 → 扣 1 支，欠籤 = 1
7. 抵 3 件，籤筒內該股已無籤 → 欠籤 = 2
8. 欠籤於下次補籤後被優先扣除
**抵分與補籤的執行順序（對應 §3.6，最高優先級）**

> 下列四項共同釘住 R-02 / R-05。任一項失敗即代表演算法有「該補而未補」的瑕疵，
> **不得上線**。測試名稱須明確標註 `SPEC-3.6`，並禁止在重構時停用。

9. **情境 B**：籤筒 `[忠, 忠, 信]`，抽中忠股、抵 2 件
   → 最終籤筒須為 9 支
   → **斷言最終籤筒支數 ≠ 1**（順序錯置時會停於 `[信]`）
9b. **情境 C**：籤筒 `[忠, 忠, 孝, 孝]`，抽中忠股、抵 3 件
   → 最終籤筒須為 9 支、欠籤[忠] 須為 0
   → **斷言最終籤筒 ≠ `[孝, 孝]`**（順序錯置時會繞過「剩 2 支同庭」規則）
   → 額外斷言：最終籤筒中不存在「支數 ≤ 2 且全部同屬一庭」的狀態
9c. **情境 A（紀錄語意）**：籤筒 `[忠, 信]`，抽中忠股、抵 3 件
   → 籤筒最終狀態兩種順序皆為 8 支，故**不可只斷言籤筒**
   → 須斷言 `offsetConsumedFromBin.jinsu === 0` 且 `offsetCarriedOver.jinsu === 2`
   → 須斷言抵分歸屬於補籤**前**的輪次
10. 補籤後扣除欠籤，使籤筒再度低於門檻 → **連鎖補籤**正確執行

**性質測試（property-based，建議）**

10b. 隨機產生 10,000 組合法籤筒狀態與抽籤參數，對每次抽籤後的籤筒斷言**不變量**：
   `籤筒支數 ≥ 2` 且 `非（支數 == 2 且兩支同庭）`
   此不變量若被破壞，即代表補籤檢查在錯誤的時點執行。

**抵分（續）**

11. 抵分範圍指定跨案類 → 另一案類籤筒亦正確扣減並各自跑補籤檢查
12. 抵分數超過 `maxOffsetPerCase` → 拒絕

**迴避**
13. 迴避 1 股 → 該股不被抽中，但其籤仍留在籤筒、支數不變
14. 迴避後 drawable 為空但籤筒非空 → 先補籤再抽
14b. **欠籤導致補籤後仍無可抽籤 → 須持續補籤直到有籤可抽**
   （由 #10b 性質測試發現：多股累積欠籤時，補籤放入的籤會立即被欠籤扣除，
   剩餘的籤可能仍全屬迴避股，補一次並不足夠。此時並非「所有股均迴避」，
   若逕行中止會給出錯誤的失敗原因。）
15. 全部在職股皆迴避 → 於補籤前即刻中止並回報錯誤

**股別變動**
16. 新增股且 N = 3 → 立即投入 3 支籤，並執行補籤檢查
17. 停用股 → 其籤立即自籤筒撤出；若因此使籤筒 ≤ 1 支 → 觸發補籤
18. 修改既有股的 N 值 → 不影響現有籤筒，自下次補籤起生效

**批次**
19. 批次 5 件，中途觸發補籤 → 後續件正確使用補籤後的籤筒
20. 批次第 3 件失敗 → 前 2 件保留，明確回報中止位置

**更正／作廢／重抽**
21. `AMEND` 不改變籤筒狀態
22. `OFFSET_AMEND` 增加抵分 → 正確補扣；減少抵分 → 正確沖銷欠籤並退還籤
23. `VOID` 最近一筆 → 籤筒完全回復至 `binBefore`
24. `VOID` 非最近一筆 → 拒絕
25. `REDRAW` 且 `redrawReturnsTicket = true` → 原籤放回、抵分沖銷、重抽排除該股

**亂數與驗證**
26. 拒絕採樣：對 n = 3、5、7 等非 2 冪值，以大量樣本驗證分布無模數偏差
27. 相同 `commitPayload` 與相同 drand 亂數 → 結果完全可重現
28. drand 簽章驗證失敗 → 中止
29. drand 全部端點不可用 → 中止，且**確認未使用本機亂數**
30. 竄改 `history.jsonl` 任一筆 → 雜湊鏈驗證失敗並正確指出位置
31. 竄改 `state.json` → 抽籤前檢查即中止

**權限**
32. 非授權帳號發動 → 拒絕並產生 `DENIED` 紀錄
33. 授權已過期（`validTo` 已過）→ 拒絕
34. `DRAW_OPERATOR` 嘗試執行 `void.yml` → 拒絕

**長期公平性（統計驗收）**
35. 模擬 10,000 次抽籤（無抵分、無迴避），各股受分件數的相對偏差應在統計容許範圍內，並以卡方檢定驗證

---

## 15. 待確認事項

以下事項需求方尚未確認，暫以括號內預設值實作，確認後修改 `config.json` 即可：

| # | 事項 | 暫定值 |
|---|---|---|
| A-01 | 4 庭與 8 股的**正式名稱**與隸屬關係 | 以刑一～刑四、忠孝仁愛信義和平為範例 |
| A-02 | 抽籤操作者的**人數與名單** | 暫定 1 主要 + 1 備援，介面已支援多人 |
| A-03 | repo 建立於**機關 Organization** 或個人帳號之下 | 建議 Organization，便於人員異動時交接 |
| A-04 | **案號公開**於網際網路是否妥適，或應改為部分遮蔽（如僅顯示末三碼）。<br>⚠ 此項不只是隱私取捨：repo 若不公開，第 4 層防護失效，<br>單人架構下將無任何機制能約束擁有者（見 §8.2.1） | 暫定完整公開 |
| A-05 | LINE 推播是否**包含案號** | 暫定包含，可一鍵關閉 |
| A-06 | 重抽時原籤**是否放回**（`redrawReturnsTicket`） | 暫定放回 |
| A-07 | 抵分數的**上限** | 暫定 10 |
| A-08 | 抽籤紀錄表列印格式是否需符合**特定公文格式** | 暫定自訂 A4 直式 |
| A-09 | 系統上線前是否需**平行試辦**（電腦與人工同時抽籤對照） | 建議試辦 2～4 週 |
| A-10 | 是否需要**紙本簽核**流程配合 | 暫不納入 |

---

## 16. 開發階段規劃

| 階段 | 內容 | 產出 | 預估 |
|---|---|---|---|
| P1 | 資料模型 + 抽籤引擎 + 全部單元測試 | 可離線執行的 CLI 引擎，第 14 章測試全數通過 | 核心 |
| P2 | drand 整合 + 兩階段承諾—開籤 + Actions workflow + repo 保護設定 | 可實際線上抽籤 | 核心 |
| P3 | 公開看板 + 歷史查詢 + 驗證頁 | 對外公開可讀 | 核心 |
| P4 | 抽籤台 + 管理頁 + 列印版面 | 授權者可自行操作 | 核心 |
| P5 | LINE 推播 + 本機同步腳本 + 離線驗證工具 | 通知與備份完備 | 加值 |
| P6 | 平行試辦、教育訓練、正式上線 | 使用手冊、操作圖卡（大字版） | 導入 |

**P1 為最關鍵階段**：抽籤演算法一旦上線就難以修改（會影響歷史一致性），必須在 P1 完成全部規則的測試驗收後才進入 P2。

---

## 17. 專案檔案結構

```
court-lottery/
├── README.md                     專案說明與快速上手
├── SPEC.md                       本規格書
├── data/
│   ├── config.json               組織與規則設定
│   ├── state.json                籤筒即時狀態
│   ├── history.jsonl             抽籤與稽核紀錄（只增不刪）
│   ├── operators.json            授權清單
│   └── pending/                  承諾階段暫存
├── engine/
│   ├── lottery.mjs               抽籤核心演算法（第 3 章）
│   ├── drand.mjs                 公共亂數取得與簽章驗證
│   ├── hash.mjs                  RFC 8785 正規化與 SHA-256
│   ├── records.mjs               紀錄產生與雜湊鏈
│   └── cli.mjs                   本機執行入口（離線可用）
├── engine/test/
│   ├── lottery.test.mjs          第 14 章測試項目
│   ├── refill.test.mjs
│   ├── offset.test.mjs
│   └── fairness.test.mjs         長期公平性統計驗收
├── public/
│   ├── index.html                公開看板
│   ├── history.html              歷史查詢
│   ├── verify.html               結果驗證
│   ├── draw.html                 抽籤台
│   ├── admin.html                組織管理
│   ├── print.html                列印用紀錄表
│   ├── css/large-type.css        大字體與無障礙樣式
│   └── js/                       前端模組（與 engine/ 共用演算法）
├── tools/
│   ├── sync-local.ps1            本機歷史同步
│   ├── verify-all.mjs            離線全量驗證
│   └── verify-all.mjs             離線全量稽核
└── .github/workflows/
    ├── draw.yml   redraw.yml   amend.yml   void.yml
    ├── config.yml  backfill.yml  resume.yml
    └── verify.yml  pages.yml
```

---

## 18. 設計理念摘要

本系統以「**任何人都不必信任本系統的任何一方**」為前提設計：

- 抽籤結果不是由本系統決定的，而是由**任何人都能查證的公共亂數**決定的。
- 抽籤標的在亂數產生**之前**就已經公開上鏈固定，所以沒有「先看結果再決定要不要算數」的空間。
- 歷史紀錄以雜湊鏈相連並存於 git，**任何一筆的竄改都會使其後所有紀錄驗證失敗**。
- 抽籤權限與資料修改權限在**技術層面**完全分離，不是靠制度約束，而是靠權杖權限與分支保護規則。
- 全部程式碼、資料與規則都是公開的純文字，任何人都可以自行重算與稽核。

因此，即使有人質疑抽籤不公，機關無須自證清白——**質疑者可以自己驗證**。這是本方案相較於任何共享文件、私有系統最根本的差異。
